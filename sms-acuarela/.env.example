# URL del backend que envia los SMS. Se lee al hacer "npm run build".
# Si no se define, la app apunta a http://localhost:3001/send-sms (solo desarrollo).
VITE_SEND_ENDPOINT=https://TU-BACKEND/send-sms
